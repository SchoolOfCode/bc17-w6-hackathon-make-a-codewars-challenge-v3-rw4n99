import { test, expect } from "vitest";

import {
  weatherPrediction
} from "./main"

test('should return true', () => {

  });

test('Stromy should be Clear', (weatherPrediction) => {
  expect(result).toBe(Clear)
})